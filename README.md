# Visualiz

A Git Gource-like visualization tool for repository history.

## Features

- Visualize git commit history as a growing tree
- Interactive controls
- Customizable settings

## Usage

1. Clone repository
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
